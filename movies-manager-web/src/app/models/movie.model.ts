export interface Movie {
  id: string,
  tconst: string,
  titleType: string,
  primaryTitle: string,
  originalTitle: string,
  isAdult: boolean,
  startYear: number,
  endYear: number,
  runtimeMinutes: Number,
  genres: string,
}

